{
  "framework": "vite",
  "buildCommand": "cd ai-workshop && npm install && npm run build",
  "outputDirectory": "ai-workshop/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
